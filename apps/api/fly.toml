app = "probable-api"
primary_region = "sfo"

[http_service]
  internal_port = 3001
  force_https = true
  auto_start_machines = true
  auto_stop_machines = true
  min_machines_running = 1
  processes = ["app"]

[env]
  PORT = "3001"
  NODE_ENV = "production"
